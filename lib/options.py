class Options:
                   
    def __init__(self, num_pixels, step, brightness, sleepytime, pixels, colors, buffer, debug):
        self.num_pixels = num_pixels
        self.step = step
        self.brightness = brightness
        self.sleepytime = sleepytime
        self.pixels = pixels
        self.colors = colors
        self.buffer = buffer
        self.debug = debug
